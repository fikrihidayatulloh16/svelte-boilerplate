Tech
- Svelte5
- shadcn-svelte
- connectRPC

struktur


src/lib/
├── gen/                <-- Kontrak Data (Generated Types dari gRPC/REST)
├── shared/
│   ├── api/            <-- Konfigurasi Global (Fetch wrapper / ConnectRPC Transport)
│   └── components/     <-- Shadcn UI (Atomic Components)
└── features/
    └── user/
        ├── api/        <-- "Kabel" spesifik fitur (ConnectRPC Client / Fetch Call)
        │   ├── grpc-client.ts    <-- Jika pakai gRPC
        │   ├── rest-client.ts    <-- Jika pakai REST (Fetch/Axios)
        │   └── index.ts          <-- Pintu keluar (Export salah satu)
        ├── components/ <-- UI spesifik fitur (UserTable, UserForm)
        └── stores/     <-- Logika reaktif Svelte 5 (UserStore.svelte.ts)


================ boilerplate feature core ===============

Agar Anda bisa langsung tancap gas ke proses bisnis (misal: fitur e-commerce, sistem sekolah, atau B2B), berikut adalah komponen wajib yang harus ada di boilerplate Anda selain gRPC:

1. Authentication Interceptors (The "Heart") ✅
Di gRPC, kita tidak memakai axios.interceptors. Kita memakai Connect Interceptors.
Mengapa wajib? Anda tidak mau menulis kode untuk mengambil token dari localStorage dan menaruhnya di header di setiap fungsi API.

Logic: Otomatis menyisipkan Authorization: Bearer <token> ke setiap request.

Logic: Menangani error 401 (Unauthenticated) secara global untuk memaksa user logout atau refresh token.

2. Global Feedback System (Toasts & Loading Bar)
Jangan biarkan user menebak apakah aplikasi sedang bekerja atau error.

Sonner (Toasts)✅: Integrasikan gRPC error codes ke dalam Toast. Jika backend mengirim error PERMISSION_DENIED, aplikasi otomatis memunculkan pesan merah "Anda tidak punya akses".

NProgress v1.0✅: Indikator loading di bagian atas layar saat navigasi antar halaman atau saat request gRPC sedang berjalan.

3. Form Handling & Validation (The "Muscle")
Bisnis itu 80% adalah mengisi form.

Zod Integration✅: Karena gRPC sudah memberi tipe data, Anda tinggal menyambungkannya dengan Zod untuk validasi di sisi klien.

Svelte-Forms-Lib (jadinya superform) / Superforms✅: Mengelola state input (dirty, touched, error) agar Anda tidak membuat variabel $state manual untuk setiap kolom input.

4. Theme & Layout Engine
Dark Mode ✅: Pasang svelte-themes. Di tahun 2026, dark mode bukan fitur bonus, tapi kewajiban.

Responsive Sidebar/Navbar ✅: Buat komponen shell aplikasi yang sudah siap dengan slot untuk profil user dan menu navigasi.

5. Deployment & Dev-Environment Consistency
Docker & Docker Compose: Karena Anda suka self-hosting di Proxmox, boilerplate Anda harus punya Dockerfile yang sudah dioptimasi untuk SvelteKit (Node.js adapter) agar sekali push, langsung jalan di server.

Environment Manager: Skema .env.example yang jelas untuk memisahkan URL gRPC backend (Local vs Production).

Komponen,Status,Mengapa Penting?
gRPC Mocking,✅ Aman,Bisa ngoding tanpa nunggu backend jadi.
Interceptors,✅ sudah,Agar tidak perlu urus token manual di setiap fitur.
Global Error Handler,✅ Belum,Agar error gRPC tidak membuat aplikasi crash.
Zod Schema,✅ Belum,Validasi input user sebelum dikirim ke backend.
Dark/Light Mode,✅ Belum,Kenyamanan visual standar industri.

==============================================================================

===================Developer Workflow Guide===========================

this Workflow if get confuse where from started

0. (only gRPC/tRPC) create contract

create contract in apps/svelte5/proto

0a. for example: auth.proto in the project


0b. generate proto buf

in root/app/svelte5
```bash

npx buf generate

```

1. Create Client

fetch raw data from gRPC or REST in api, the example is in apps/svelte5/src/lib/features/user/api/grpcClient.ts

2. Create Zod Schema (if mutation)

create to sanitize the data apps/svelte5/src/lib/features/user/ or apps/svelte5/src/lib/features/user/schema

the example apps/svelte5/src/lib/features/user/schema/user.schema.ts

3. Create Service 

create service to decoupled the raw fetch whenever using gRPC, REST, etc in api

4. Create query/mutation

recommended to Create query/mutation if the project has mutation like get list, post, etc in api, if not continue to creaate UI components

example is in apps/svelte5/src/lib/features/user/api/queries.ts

5. Create UI Components

this is where the UI component developed

6. Show to the page

after component create present it in routes

==============================================================================

===================change auth and user to rest===========================

dengan arsitektur Service Layer Anda saat ini, inilah yang akan Anda lakukan dengan santai sambil minum kopi:

Bikin file baru: restClient.ts.

Buka auth.service.ts dan user.service.ts.

Ubah import-nya dari import { authClient } from './grpcClient' menjadi import { authRestClient } from './restClient'.

Sesuaikan sedikit pemetaan (.map()) di dalam service jika format nama JSON vendornya berbeda.

Selesai. Anda tidak menyentuh satu pun file .svelte, queries.ts, atau +page.server.ts.

==============================================================================

===================Dark Mode Guide===========================

#############################################################

Prinsip 1: Mentalitas "Pasangan Warna"

Ini adalah kunci utamanya. Jangan pernah mendesain sebuah elemen hanya dengan warna terang.

- Pikirkan: "Jika ini putih di siang hari, apa warnanya di malam hari?"

- Praktik: Setiap kali Anda mengetikkan class warna latar belakang (bg-), warna teks (text-), atau warna batas (border-), segera ketikkan juga pasangan dark:-nya.

Contoh:

- bg-white Pasangannya dark:bg-gray-900

- text-gray-900 Pasangannya dark:text-white

border-gray-200 Pasangannya dark:border-gray-700

############################################################

Prinsip 2: Gunakan "Aksen Utama" yang Fleksibel

Aplikasi Anda pasti punya warna utama (misal: Biru/blue-600).

- Di mode terang, warna terang seperti blue-600 terlihat jelas.

- Di mode gelap, warna yang terlalu pekat kadang "tenggelam".

Tips: Gunakan shade (tingkat kecerahan) yang sedikit berbeda untuk mode gelap.

- Tombol Primer: bg-blue-600 hover:bg-blue-700 dark:bg-blue-500 dark:hover:bg-blue-600

- Teks Tautan (Link): text-blue-600 dark:text-blue-400

#############################################################

Prinsip 3: Pewarnaan Dasar (Base Colors) di Global CSS

Anda tidak perlu (dan tidak boleh) menuliskan warna latar belakang <body> berulang kali di setiap halaman. Biarkan Tailwind menanganinya di level global (file src/routes/layout.css atau app.css).

Kita sudah melakukannya sebelumnya dengan menambahkan class di app.html, tetapi pendekatan yang lebih "Tailwind" adalah dengan mendefinisikannya di @layer base pada CSS Anda.

Jika Anda ingin mencoba pendekatan ini (opsional, tapi disarankan untuk kebersihan HTML), buka kembali layout.css dan tambahkan ini:

```CSS
@import "tailwindcss";
@custom-variant dark (&:where(.dark, .dark *));

/* Tambahkan Base Layer untuk "Kanvas Utama" */
@layer base {
    body {
        @apply bg-gray-50 text-gray-900 dark:bg-gray-950 dark:text-gray-100;
    }
}
```

Jika Anda melakukan ini, Anda bisa menghapus class bg-gray-50 text-gray-900 ... dari tag <body> di app.html Anda. Ini akan menjaga HTML tetap bersih, dan kanvas utama akan selalu diatur secara otomatis oleh CSS.

############################################################

Prinsip 4: Komponen Reusable (Atomik)

Saat Anda membuat komponen seperti tombol, kartu, atau input, definisikan gaya dark mode-nya langsung di dalam komponen tersebut.

Misalnya, komponen <Card>:

```Svelte
<div class="rounded-xl border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-800 dark:bg-gray-900">
    {@render children()}
</div>
```

==============================================================================

===================perlu disesuaikan===========================

jika boilerplate ini terbukti kokoh, ketika ingin dipakai dengan api atau backend yang asli, yang diubah hanya service, transport tapi cek berikut

###################################
// apps/svelte5/src/hooks.server.ts

if (token) {
        // --- BAYANGKAN LOGIKA INI NANTINYA ---
        // const grpcResponse = await userClient.getProfile({ token });
        // ATAU
        // const restResponse = await fetch('/api/profile').then(r => r.json());

        // --- UNTUK SEKARANG (MOCKING) ---
        // Kita masukkan objek murni biasa. Bebas tambah properti apa saja!
        event.locals.user = {
            id: '1',
            fullName: 'Fikri Hidayatulloh',
            role: 'admin',      // <-- Tidak akan error lagi!
            email: 'fikri@example.com'
        };
    } else {
        event.locals.user = null;
    }

######################################
// apps/svelte5/src/hooks.server.ts

const authHandler: Handle = async ({ event, resolve }) => {
    const token = event.cookies.get('session_token');

    if (token) {
        // --- BAYANGKAN LOGIKA INI NANTINYA ---
        // const grpcResponse = await userClient.getProfile({ token });
        // ATAU
        // const restResponse = await fetch('/api/profile').then(r => r.json());

        // --- UNTUK SEKARANG (MOCKING) ---
        // Kita masukkan objek murni biasa. Bebas tambah properti apa saja!
        event.locals.user = {
            id: '1',
            fullName: 'Fikri Hidayatulloh',
            role: 'admin',      // <-- Tidak akan error lagi!
            email: 'fikri@example.com'
        };
    } else {
        event.locals.user = null;
    }

    return resolve(event);
};

####################################

################################
// apps/svelte5/src/lib/shared/api/transport.ts

// Ganti ke 'false' nanti jika Backend Nginx/Go/NestJS Anda sudah siap
const USE_MOCK = true; 

#################################

################################
// apps/svelte5/src/routes/auth/login/+page.server.ts
export const actions = {
    login: async ({ request, cookies }) => {
        const data = await request.formData();
        const email = data.get('email');
        const password = data.get('password');

        // --- MOCK LOGIC: Pura-pura validasi ke Backend ---
        // Jika di dunia nyata, di sini Anda melakukan:
        // const res = await fetch('http://api.fikri.com/login', { ... })

        //  SESUAIKAN APAKAH TOKEN ATAU COOKIE

        // const token = res.json().token;

        if (email === 'admin@example.com' && password === 'admin123') {
            // Berhasil login! Kita tanam Cookie session_token
            cookies.set('session_token', 'mock-jwt-token-xyz-789', {
                path: '/',
                httpOnly: true, // Tidak bisa dibaca XSS hacker
                sameSite: 'strict',
                secure: process.env.NODE_ENV === 'production',
                maxAge: 60 * 60 * 24 // 1 Hari
            });

            // Redirect ke halaman terproteksi
            throw redirect(303, '/dashboard');
        }

        // Jika gagal login
        return fail(401, { error: 'Email atau password salah!' });
    }
} satisfies Actions;

#################################

#################################

3. Sparring Intelektual: Bagaimana "Membatalkan" Secara Nyata?
Tombol "Batalkan" di atas saat ini baru membersihkan UI. Namun, di balik layar, request API (REST/gRPC) atau Navigasi SvelteKit mungkin masih berjalan. Agar boilerplate Anda benar-benar tangguh, Anda perlu memahami konsep AbortController.

Untuk Request API:
Anda harus mengirimkan AbortSignal ke fungsi fetch atau gRPC Anda.

// Contoh implementasi di logic Anda
const controller = new AbortController();

// Di LoadingStore.cancelAll()
controller.abort(); 

// Di API Call
const response = await fetch('/api/data', { signal: controller.signal });

##########################

#############################

// apps/svelte5/src/lib/features/auth/api/auth.grpcClient.ts

// 3. SWITCHER (Tinggal ubah true/false saat backend asli sudah siap)
const USE_MOCK = true;

// Ini yang di-export dan dipakai oleh +page.server.ts
// Nanti kalau backend siap, tinggal ganti USE_MOCK = false.
export const authClient = USE_MOCK ? mockClient : realClient;

############################

==============================================================================

===================perlu diuji===========================

- apakah bisa memakai backend apapun tanpa banyak Konfigurasi

- apakah keamanan komunikasi data, seperti aman dari httpcanary

==============================================================================

========================= The Absolute Rule keep clean aadn decoupled ============

Cek Ombak: Aturan Main untuk "File Lainnya"
Agar arsitektur ini tetap decoupled, setiap file sekarang memiliki aturan ketat tentang apa yang boleh dan tidak boleh diimpor.

1. Komponen UI (.svelte files)
Tugas: Menampilkan data dan menangani klik user.

Boleh Tahu: Hanya skema Zod (Domain Entity), misalnya UserEntity atau LoginFormData.

HARAM Tahu: Tidak boleh ada satupun import dari $lib/gen/proto/... atau hal-hal berbau gRPC/REST. Jika ada komponen Svelte yang mengimpor proto, itu artinya UI Anda "bocor" ke infrastruktur.

2. TanStack Query (queries.ts / mutations.ts)
Tugas: Mengurus caching, loading state, dan retry di sisi klien.

Boleh Tahu: Hanya memanggil fungsi dari Service (contoh: userService.fetchAll()).

HARAM Tahu: Tidak boleh memanggil grpcClient atau fetch secara langsung. Jika Query langsung menembak API, Service layer Anda jadi tidak berguna.

3. SvelteKit Server Actions (+page.server.ts)
Tugas: Menerima form HTML, memvalidasi dengan Superforms/Zod, mengatur Cookie.

Boleh Tahu: Memanggil fungsi dari Service (contoh: authService.login()) dan tipe dari skema Zod.

HARAM Tahu: Sama seperti UI, tidak boleh memanggil grpcClient atau melakukan parsing data kotor dari backend.

4. Service (service.ts)
Tugas: Sang Penerjemah. Meminta data kotor ke Client, merapikannya (Mapping/Zod Parsing), lalu mengembalikannya dalam bentuk yang bersih ke UI/Query.

Boleh Tahu: Segala hal. Dia tahu gRPC, dia tahu REST, dia tahu Zod. Dialah yang memikul beban kerumitan agar file lain bisa hidup tenang.

5. Client / Transport (grpcClient.ts / transport.ts)
Tugas: Sang Kurir. Hanya bertugas mengirim dan menerima byte data lewat internet atau memanggil data palsu dari Mock.

Boleh Tahu: Hanya tahu protokol gRPC (file .proto) dan koneksi HTTP. Tidak peduli soal Zod, UI, atau aturan bisnis.

=========================== Current Contract ===========================

######################## 1. auth contract #############################
syntax = "proto3";

package v1;

// Objek data UserLogin
message LoginRequest {
    string email = 1;
    string password = 2;
}

// Kontrak jawaban dari server (Response)
message LoginResponse {
  string session_token = 1;
  UserLoginResponse user = 2;
}

message UserLoginResponse {
  string id = 1;
  string email = 2;
  string full_name = 3;
  string role = 4;
}

message LogoutRequest {
  // Kosong tidak masalah! 
  // Di gRPC web/SvelteKit, session_token biasanya dikirim lewat 
  // Header (Metadata), bukan lewat body request agar lebih aman.
}

message LogoutResponse {
  bool success = 1;
  string message = 2;
}

// Definisi layanan (Service)
service AuthService {
  rpc Login(LoginRequest) returns (LoginResponse);
  rpc Logout(LogoutRequest) returns (LogoutResponse);
}

############################################

##################  2. user contract #############
syntax = "proto3";

package v1;

// Objek data User
message User {
    // WARNING: DO NOT CHANGE THIS STRUCT NO MATTER WHAT!!!
    string id = 1;
    string email = 2;
    string full_name = 3;
    string avatar_url = 4; // Commonly use
    bool is_active = 5;

    // use the correct data type
    string created_at = 6; 
    string updated_at = 7;

    // use this next beneath to spesific business, such as paid_status, OR loyalty_points
}

// Kontrak permintaan data (Request)
message GetUsersRequest {
  int32 page = 1;
  int32 limit = 2;
  string search = 3;
}

// Kontrak jawaban dari server (Response)
message GetUsersResponse {
  repeated User users = 1;
  int32 total_count = 2;
}

// Definisi layanan (Service)
service UserService {
  rpc GetUsers(GetUsersRequest) returns (GetUsersResponse);
}

#################################################

=======================================================================